# PHP под капотом. Карта мира PHP 🐘

Привет всем заглянувшим.  
Сегодня на повестке дня — составление карты мира PHP.  
Ее цель - сформировать фундаментальное понимание этой технологии  
и ментально пройти путь от кода к процессу,  
чётко осознавая роль каждого звена этой цепи.
Начнем.


## Введение

(Слайд 1)
Около десяти лет назад у меня появился вопрос: а что такое PHP? То есть мне захотелось узнать детали работы этого инструмента. Составить ясную картину опираясь на официальну документацию у меня не вышло. Хотя документация у PHP одна из лучших. В тот момент мой опыт в программировании был очень поверхностным. Но для меня и тогда существовала связь между комфортом использования инструмента и глубиной понимания его устройства. Поэтому я взялся копать.  
В процессе использования языка и размышлений о нём у меня сложился определённый пазл.  
Этот пазл мы сейчас и будем собирать.  
Итак...  
У нас есть:

### 1. Классическая ситуация. Компилируемые языки.

(Слайд 2)
```bash
Исходный код на ЯП → Компилятор → Исполняемый файл (программа) → Запущенный процесс (исполняющий инструкции программы в контексте данных программы). 
```

> **Дополнительная информация!**
>
> Процесс — это активная сущность: программа в состоянии исполнения.  
> Но на уровне ядра, процесс — это структура данных, описывающая всё, что нужно,  
> чтобы программа могла исполняться (или быть готовой к исполнению).  
> Я планирую записать видео о разнице между процессом и программой. Как только сделаю это, добавлю ссылку в закрепленный комментарий.   

(Слайд 8)
### 2. Альтернативный подход. Интерпретируемые языки.
В контексте классики может показаться, что PHP — не совсем язык программирования, так как не существует исполняемого файла-программы, являющегося результатом компиляции кода на PHP.

(Слайд 9)
Для программиста даже не существует компилятора PHP-кода (хотя особый вид компилятора есть и у PHP. Он запускается как часть процессов интерпретации). То есть особенность заключается в том, что интерпретируемые языки просто изменяют классическую последовательность действий.

Теперь у нас так:

(Слайд 10)
- Начинается всё с запуска интерпретатора. 

**Интерпретатор** — это готовая к исполнению программа, сама по себе ничего полезного нам не дающая, но имеющая набор средств для интерпретации кода на PHP. 

(Слайд 11)
Если просто запустить интерпретатор PHP в терминале, но не передать путь к файлу:

```bash
php
```
(Слайд 12)
То он переключится в интерактивный режим. То есть будет ожидать ввода скрипта с клавиатуры напрямую. 

> Заметка!
>
> В качестве конца ввода скрипта требуется послать EOF (Ctrl+D), чтобы терминал прервал чтение и отправил данные в PHP.

(Слайд 13)
Если передать корректный PHP-код, то он попадёт ядру на интерпретацию. Результат интерпретации будет направлен в stdout (Слайд 14).

*А если входящая строка без тега, то интерпретатор воспринимает её как обычную строку HTML и просто возвращает её, ничего с ней не делая (результат аналогичен cat).*

**В общем, интерпретатор так или иначе, но ждёт описанный на языке PHP алгоритм действий. Без этого он бесполезен.** 

(Слайд 15)
Следовательно, при стандартном подходе мы запускаем на исполнение интерпретатор и даём в качестве параметра путь к сценарию.  
(Слайд 16)  
У нас появляется процесс, исполняющий интерпретатор.  
(Слайд 17) (Слайд 18)  
Который получил путь к файлу, в котором описан код (например, который мы ранее писали в интерактивном режиме).  
(Слайд 19)  
Интерпретатор выполнит наш алгоритм и вернёт строковый результат в stdout.  
И завершится. То есть на этом его работа полностью закончится. 

*Тут важно уловить, что операционная система запускает именно процесс интерпретатора.  
То есть реально работает программа, написанная на C и скомпилированная в исполняемый бинарник. Она принимает путь к исходному коду на PHP и считывает его. Она преобразует его в некое подобие бинарника внутри себя. То есть парсит, преобразует абстрактное синтаксическое дерево и компилирует это всё в опкоды. А затем, прямо в себе, исполняет их.  
Это просто иная схема исполнения: нет автономного исполняемого файла, но есть финальный смысловой результат — процесс.  
И алгоритм, исполняемый этим процессом, описан на языке программирования PHP.*  

(Слайд 20)
Резюме:

1. PHP-код сам по себе не превращается в отдельный бинарник — он всегда исполняется внутри процесса интерпретатора.  
2. Интерпретатор не содержит ни бизнес-логику, ни прикладной код. Он обеспечивает только механизмы исполнения.

В общем-то, понимание даже этого уровня уже вполне достаточный фундамент для полноценной работы с PHP. Но иногда... 

(Слайд 21)
## Нужно больше ~~золота~~ знаний. 

Давайте попробуем составить смысло-логический список сущностей для будущей карты.  
И начнём с того, что поймём: PHP — это:

(Слайд 22)(Слайд 23)
### **Язык.** 
Набор правил по составлению исходного кода, пригодного для интерпретатора PHP. Они описаны в неформальной спецификации php.net/manual/en/.  
Если скрипт соответствует этим правилам, то его сможет принять интерпретатор. 

(Слайд 24)
### **Интерпретатор.**
В широком смысле — совокупность программных средств, написанных на C, позволяющая:

- принять сценарий и данные для него,
- интерпретировать и выполнить описанное в сценарии.

(Слайд 25) 
#### Три основных компонента интерпретатора:

```bash 
1. (слайд 26) Интерфейс SAPI -> 2. (слайд 27) Движок Zend Engine -> 3. (слайд 28) Система расширения функций движка 
```

##### 1. SAPI (Server API) 

(Слайд 29)
Тут мы видим SAPI как некий механизм — программный слой между ядром и данными процесса. Но я хочу сначала посмотреть на SAPI глазами программиста.

###### С точки зрения программиста

SAPI — это профиль окружения. То есть конкретный набор наполненных суперглобальных массивов и способы работы с I/O.

(Слайд 30)
Например, когда вы работаете с PHP-кодом в вебе (например, php-fpm), то у вас есть привычный набор суперглобальных массивов:  

- $_SERVER  
- $_ENV  
- $_REQUEST  
- $_GET  
- $_POST  
- $_COOKIE  
- $_FILES  
- $_GLOBALS  

Но если вы запустите код в терминале, используя консольный интерпретатор PHP, то обнаружите, что заполнены только два суперглобальных массива, а остальные пусты.

Это и есть SAPI. Точнее, это результат его работы. 

(Слайд 31)
**Для программиста SAPI — это профиль окружения, определяющий как комбинацию заполненных и пустых суперглобальных массивов, так и доступные механизмы ввода-вывода (например, STDIN/STDOUT/STDERR или потоки php://stdin и т. д.), характерные для конкретной среды запуска.**

SAPI создаёт соответствующий среде профиль окружения.  
Существует несколько сред запуска, а значит, и несколько возможных SAPI.  

Посмотрим на это немного конкретнее.

(Слайд 32)
**Интерпретатор PHP для терминала, скомпилирован с CLI SAPI:**  
А значит, SAPI наполнит только два контейнера (массива), то есть создаст условно "CLI-профиль". Это:  
- $_SERVER (установлен и наполнен)  
- $_ENV (установлен и наполнен) (если в php.ini включено variables_order с буквой E — по умолчанию "EGPCS")  
- $_REQUEST (установлен, но пуст)  
- $_GET (установлен, но пуст)  
- $_POST (установлен, но пуст)  
- $_COOKIE (установлен, но пуст)  
- $_FILES (установлен, но пуст)  
- $_GLOBALS (установлен, но пуст)  

I/O: константы (STDIN, STDOUT, STDERR) или псевдопотоки (php://stdin, php://stdout, php://stderr).

(Слайд 33)
**Интерпретатор для веба, php-cgi, который скомпилирован с CGI SAPI:**

Тогда SAPI заполнит все массивы, то есть создаст условно "веб-профиль".  
- $_SERVER (установлен и наполнен)  
- $_ENV (установлен и наполнен) (если в php.ini включено variables_order с буквой E — по умолчанию "EGPCS")  
- $_REQUEST (установлен и наполнен)  
- $_GET (установлен и наполнен)  
- $_POST (установлен и наполнен)  
- $_COOKIE (установлен и наполнен)  
- $_FILES (установлен и наполнен)  
- $_GLOBALS (установлен и наполнен)  

I/O: псевдопотоки php://input, php://output, php://stderr.  
Константы STDIN, STDOUT, STDERR, как правило, не объявлены (`var_dump(defined('STDIN'), defined('STDOUT'), defined('STDERR'));`). 

(Слайд 34)
**Интерпретатор для веба php-fpm который скомпилирован с FPM SAPI:**

Это особый вид SAPI. Это уже не просто профиль окружения, а целая полновесная система. 
Она состоит из демона который слушет подключения через сокет или по сети, внутри которого есть мастер процесс и еще пул запущенных интерпретаторов (воркеров). SAPI распределяет запросы между этими  воркерами.  И осуществляет обмен данными с веб-сервером сделавшим к нему запрос по протоколу fast cgi.
Но для программЭто полноценный веб-профиль с точки зрения окружения:

- $_SERVER (установлен и наполнен)  
- $_ENV (установлен и наполнен) (если в php.ini включено variables_order с буквой E — по умолчанию "EGPCS")  
- $_REQUEST (установлен и наполнен)  
- $_GET (установлен и наполнен)  
- $_POST (установлен и наполнен)  
- $_COOKIE (установлен и наполнен)  
- $_FILES (установлен и наполнен)  
- $_GLOBALS (установлен и наполнен)  

I/O: псевдопотоки php://input, php://output, php://stderr.  
Константы STDIN, STDOUT, STDERR, как правило, не объявлены (`var_dump(defined('STDIN'), defined('STDOUT'), defined('STDERR'));`). 

> **Дополнительная информация!**
> 
> Директива variables_order в файле php.ini определяет одновременно два параметра:
>
> - Порядок, в котором интерпретатор будет наполнять суперглобальные массивы.  
> - Отдельно для массива $_ENV определяет, будет ли он заполняться. Если в значении нет буквы `E`, то не будет.  
> ```bash
> variables_order="EGPCS"
> ```

(Слайд 35)
###### С технической точки зрения

**SAPI — это интерфейс, в запущенном интерпретаторе, между ядром Zend Engine и данными, пришедшими в процесс из среды, сделавшей вызов интерпретатора.** 

Это справедливо, если речь идёт о запуске в консоли или уже вышедшем из обихода CGI. То есть когда просто запускается процесс php или php-cgi, выполняет инструкции и завершается.  
При использовании php-fpm это уже нечто большее — демон, свой протокол обмена сообщениями, мастер-процесс и пул воркеров (интерпретаторов), которые оркестрирует этот мастер-процесс.  

На практике, как правило, сейчас используется php-fpm и php (терминал), а в продвинутых случаях — те или иные версии асинхронного PHP. Я буду их разбирать, но не сейчас. CGI же я упоминаю только для понимания логики процесса. Исторически CGI был первым вариантом PHP.  
И он был построен на принципах, которые использовались и используются по сей день при стандартном запуске программ в терминале.  

(Слайд 36)  
То есть изначально существовал некий принцип запуска программ в терминале.  
(Слайд 37) На основе этого принципа был разработан способ запуска PHP-кода через веб-сервер (php-cgi).  
(Слайд 38) А потом на этом же принципе был сделан интерфейс для запуска PHP-кода и в терминале.  

Давайте посмотрим на эти принципы и поймём общую логику. 

(Слайд 39)  
Допустим, у нас есть скрипт на bash и есть процесс, в котором запущен терминал с оболочкой bash. В этом процессе мы вызываем что-то типа:  
(Слайд 40)

```bash
bash ./script_name.bash
```

Оболочка bash понимает, что мы хотим запустить скрипт на исполнение, и она делает некоторые подготовительные действия и системный вызов fork() (Слайд 41). То есть она обращается к ОС и говорит: создай мою копию.  

(Слайд 42)  
ОС копирует структуру этого процесса оболочки в своей памяти.  
В этот дочерний процесс попадает всё от родителя (все данные — полная копия, но в режиме Copy-on-Write), включая потоки ввода-вывода.  

(Слайд 43)  
После fork() у нас два процесса:

- Родитель (исходный bash) — ждёт завершения потомка;  
- Дочерний — точная копия bash в этот момент.  

То есть запуск fork() в родителе закончился возвратом в два процесса. И теперь они оба продолжают работать параллельно, начиная с точки возврата значения функцией fork().  

**Один вызов — два возврата!**

(Слайд 44)
В виде псевдокода это выглядит так:
```php
function startExecve(bin, params, env)
{ 
    pid = fork(); // Раздвоение!
    if (pid == 0) { 
        // Этот блок выполняется только дочкой 
        execve(bin, params, env); 
    } else { 
        // Этот блок выполняется только родителем 
        waitpid(pid, ...); 
    } 
} 

if (isCallForExecution) { // В родительском процессе
    // Важно понять, что в потенциале тут может быть указан любой интерпретатор (например, php)
    // и путь может быть указан к условному script_name.php
    params = ["bash", "./script_name.bash"];
    bin = "/usr/bin/bash";
    env = environ;
    startExecve(bin, params, env); 
}
```

(Слайд 45)
В общем, внутри дочернего процесса вызывается системный вызов execve("/usr/bin/bash script_name.bash", [env], [params]).  
Это говорит ОС, чтобы она взяла бинарник `/usr/bin/bash` и заменила им инструкции и данные в памяти дочернего процесса.  
А также указывает, что должно быть у него в качестве параметров вызова и переменных окружения. (Слайд 46)  

> Важно!
>
> В итоге дочерний процесс имеет копию файловых дескрипторов родителя, переменные окружения и параметры, переданные при вызове.  
> Это стандартный набор данных для передачи интерпретатору.  
> А также запущенный в себе экземпляр интерпретатора bash и скрипт с алгоритмом на языке bash.  

Именно эта схема была взята за основу при разработке php-cgi.  
Он предназначался для запуска веб-сервером, то есть в контексте протокола HTTP.  
Если совсем коротко про HTTP — это протокол, определяющий правила общения между клиентом и сервером в вебе.  
У него есть ряд методов: GET, POST, PUT, PATCH, DELETE, HEAD, OPTIONS, TRACE, определяющих смысловое намерение запроса,  
и общепринятый вид запроса, определяемый методом. 

(Слайд 47) (Слайд 48).
```http
GET /search?q=php+http+request&page=2 HTTP/1.1​
Host: example.com​
User-Agent: Mozilla/5.0 (X11; Linux x86_64)​
Accept: text/html,application/xhtml+xml,application/xml;q=0.9,*/*;q=0.8​
Connection: keep-alive
```
```http
POST /api/login HTTP/1.1​
Host: example.com​
User-Agent: curl/8.6.0​
Content-Type: application/json​
Content-Length: 53​
Accept: application/json​

{​

  "username": "vitaliy",​

  "password": "secret123"​

}
```
При HTTP-запросе, адресованном к PHP-файлу, сервер парсит стартовую строку и HTTP-заголовки и раскладывает их в переменные окружения.  
А тело запроса, если оно есть, помещает в stdin. Далее всё идентично процессу вызова в терминале: веб-сервер делает вызов fork().  
Потом внутри дочернего процесса вызывается execve("/usr/bin/php-cgi", [env], [params]).  
ОС также заменяет память дочернего процесса инструкциями и данными бинарника /usr/bin/php-cgi и передаёт ему переменные окружения и параметры.  

При использовании CGI веб-сервер в параметрах передаёт только имя запрошенного на исполнение файла.  
То есть в процесс запущенного php-cgi данные попадают так же и те же, как и в случае запуска, например, bash-скрипта на исполнение в терминале — то есть по общему принципу.  

Позже было сделано ещё CLI SAPI. Это позволило запускать PHP-код в терминале на основе этой же логики.  

Далее, когда изобрели менеджер процессов php-fpm, то решили сделать так, чтобы SAPI FPM предоставляло тот же набор массивов, как и классический CGI.  
То есть, чтобы программист мог использовать один и тот же код, что был для php-cgi, при использовании php-fpm.  

> **Дополнительная информация!**  
> - Список доступных SAPI находится в [исходном коде PHP](https://github.com/php/php-src/tree/master) в директории `sapi`.  
> - При ручной сборке интерпретатора можно указать, с каким SAPI его собирать.  
> - Чаще используют уже собранные варианты (например: php, php-cgi, mod_php, php-fpm).  

##### 2. Ядро (Zend Engine)
 
(Слайд 49)
*Тут я не буду уходить так далеко, как с SAPI, так как ему будет посвящён самостоятельный разбор.* 
 
Оно отвечает за:

- разбор исходного кода (парсинг → AST),  
- компиляцию AST в опкоды,  
- исполнение этих опкодов виртуальной машиной.  

В узком смысле слова ядро (Zend Engine) и есть интерпретатор. То есть это мозг и руки интерпретатора.  

##### 3. Набор расширений

(Слайд 50)

Существует специальная система модульного расширения функционала ядра Zend Engine.  
Extensions — это расширения, которые собираются вместе с ядром и SAPI в бинарник.  
Другие расширения могут подключаться отдельно, как динамические модули (.so), через php.ini к уже скомпилированному интерпретатору.  
То есть подключаются в процессе запуска, а не компиляции интерпретатора.  

Эти модули — надстройки над ядром (Zend Engine),  
которые используют его API, чтобы добавить новые функции, классы, интерфейсы, типы, обёртки над системными библиотеками и т. д.  

🔹 То есть они действительно расширяют возможности ядра,  
🔹 но не являются его частью.  

Тему расширений мы тоже затронем достаточно детально, но не сейчас.

#### Резюме по интерпретатору

(Слайд 51)

Финальный вид интерпретатора — это, как правило, скомпилированный из исходников на C бинарный файл.  
Существует несколько основных видов скомпилированных интерпретаторов:  
- php (для запуска кода в терминале),  
- php-cgi (для запуска кода в качестве серверного языка) (устаревший подход),  
- mod_php (динамический модуль для Apache, а не отдельный бинарник) (особый вид интерпретатора, работающего в процессе Apache) (устаревший подход),  
- php-fpm (демон с FastCGI) (стандарт использования де-факто).  

Иными словами, это результат компиляции ядра (Zend Engine) и его расширений с различными вариантами SAPI.  

*Вариант ядра один, а вот интерфейсов для его запуска — несколько.*

## Общий план тем (Карта)

Поняв, что PHP — это набор правил по составлению кода и обвязка парадигм и технологий, позволяющих делать более сложные вещи, а также интерпретатор как совокупность технологий по исполнению этого кода, мы можем составить карту этого мира и проводить изучение каждой отдельной области до нужного уровня понимания и через это собирать свой пазл PHP.

Выглядеть это может так:

1. Среды исполнения и архитектуры взаимодействия:  
    - Основные среды исполнения PHP-кода (CLI, веб — от CGI к FastCGI). Обзор на практике.  
    - Консольные утилиты на PHP.  
    - Веб: Nginx + PHP-FPM.  
    - Веб (альтернативные способы): встроенный сервер PHP, асинхронные решения (RoadRunner, AMPHP, Swoole). Общий обзор.  
    - ...   
    
2. Работа с ядром:  
    - Этапы исполнения (парсинг → AST → опкод (байткод) → выполнение).  
    - Способы ускорения и оптимизации (OpCache, JIT, кеширование).  
    - Работа с расширением функционала ядра. Все способы установки расширений.  
    - ...    

3. Работа с кодом:  
    - Основные приёмы и паттерны для прикладных задач.  
    - Средства тестирования и контроля качества (PHPUnit, PHPCS, Psalm и др.).  
    - Composer и пакетная экосистема.  
    - ...  

4. Фреймворки:  
    - Общая картина и Laravel как частный пример.  
    - Создание пакетов и расширений для Laravel.  
    - ...

5. PHP в контексте ОС:  
    - Как PHP взаимодействует с системой (процессы, ресурсы, сокеты, system calls, файловая система, I/O).  
    - ...

В этой серии видео я буду погружаться в разные области и проводить там исследование, а потом публиковать видеоотчёт.  
Строгого порядка следования не будет — всё по воле вдохновения.  
Вы можете либо следовать со мной, либо самостоятельно копать в разные направления и составлять свой личный пазл понимания PHP на основе этой карты.

## П.с.

Вот это всё — картина внутреннего устройства мира PHP.  
Но в качестве вишенки на торте давайте немного отдалимся от PHP, чтобы увидеть ещё более глобальную карту.  
PHP — лишь часть экосистемы, существующей в большом пространстве операционной системы.  

*Это лишь одна из рыбок в аквариуме.*  

Пространство операционной системы гораздо шире, чем PHP.  

Фундаментально работа ведётся внутри ОС. Она функционирует на физическом сервере.  
Внутри ОС (как правило, семейства Linux) одной из технологий выступает веб-сервер, который принимает сообщения из внешнего мира по сети.  
За спиной этого веб-сервера работает PHP, чаще всего используемый как связующее звено между веб-сервером и СУБД.  
Хотя в роли такого звена нередко выступают и другие серверные языки.  

То есть главное "поле битвы" — операционная система.  
А уже в ней конфигурируются наборы технологий, работающих по её законам и решающих прикладную задачу.  

В этой серии мы преимущественно будем говорить о PHP,  
но, возможно, местами придётся выходить за его рамки — ради ясности.  

Удачи в вашем поиске знания.  

